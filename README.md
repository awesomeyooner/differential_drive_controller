# differential_drive_controller

This is a [ros2_control](https://control.ros.org/jazzy/index.html) controller for a custom implementation of `diff_drive_controller`. It has practically the same functionality, I just wanted to try and write my own to learn more about using `ros2_control`

ros2_control package for a custom implementation of the diff_drive_controller
